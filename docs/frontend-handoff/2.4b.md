# Frontend handoff — 2.4a/2.4b Build executor posture

Operator-only. It has no home until the operator control room (3.10, parked) exists, so do not build it before then. Build failures from the sandbox already surface through the existing build views.

Wanted: for each builder, show the executor (`development` or `hardened`), and show a non-isolating executor as an explicit warning rather than a neutral label. For hardened builders, also show the sandbox image, CNI network, and cache mode. Source it from the existing builder record or health endpoint (see `BuilderStartupContract`), not a new per-executor RPC.
